import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Profesional Remis',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
